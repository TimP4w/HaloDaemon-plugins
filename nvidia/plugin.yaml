# SPDX-License-Identifier: GPL-3.0-or-later
id: nvidia
name: NVIDIA GPUs
version: 1.0.0
license: GPL-3.0-or-later
description: NVIDIA GPU thermal sensors reported by nvidia-smi.
translations:
  it:
    plugin.name: GPU NVIDIA
    plugin.description: Sensori termici delle GPU NVIDIA rilevati da nvidia-smi.
platforms: [linux, windows]
permissions: [command]
capabilities: [sensors]
dynamic_children: true
devices:
  - vendor: NVIDIA
    model: any
    type: sensor
    match:
      command: nvidia-smi
transports:
  command:
    commands: [nvidia-smi]
