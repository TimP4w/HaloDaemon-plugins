# NVIDIA SMI

The NVIDIA plugin invokes only the manifest-declared `nvidia-smi` executable,
without a shell. GPU UUIDs form stable sensor identifiers; unavailable (`N/A`)
temperature columns are ignored.
